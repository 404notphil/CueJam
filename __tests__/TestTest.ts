// Import the expect and test functions from Jest
const {expect, test} = require('@jest/globals');

// Define a test case with 'test' or 'it'
test('asserts true', () => {
  // Expect statement to assert true
  expect(true).toBe(true);
});
